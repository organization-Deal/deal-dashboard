import fs from "node:fs";
import path from "node:path";
import { execFileSync } from "node:child_process";
const root=process.cwd();
const indexFile=path.join(root,"index.html");
const jsFile=path.join(root,"assets","dashboard.js");
const cssFile=path.join(root,"assets","dashboard.css");
const MARK="RUBJAI_RECON_EVIDENCE_DASHBOARD_V905_20260820";
for(const f of [indexFile,jsFile,cssFile])if(!fs.existsSync(f))throw new Error("v9.05 missing "+path.relative(root,f));
let html=fs.readFileSync(indexFile,"utf8"), js=fs.readFileSync(jsFile,"utf8"), css=fs.readFileSync(cssFile,"utf8");
const CARD="\n        <section class=\"recon-evidence-card\" id=\"reconEvidenceCard\">\n          <div class=\"recon-evidence-head\">\n            <div>\n              <div class=\"head-kicker\">RECONCILIATION EVIDENCE</div>\n              <h3>หลักฐานการกระทบยอด</h3>\n              <p>เก็บ Statement ต้นทาง ผู้ที่ยืนยัน เวลา ส่วนต่าง และเหตุผลของรายการค้างไว้ตรวจย้อนหลังได้</p>\n            </div>\n            <div class=\"recon-evidence-actions\">\n              <span class=\"recon-evidence-state\" id=\"reconEvidenceState\">ยังไม่มี Statement</span>\n              <button class=\"btn\" id=\"reconEvidenceCsv\" type=\"button\">ส่งออก CSV</button>\n              <button class=\"btn solid\" id=\"reconEvidencePrint\" type=\"button\">พิมพ์ / PDF</button>\n            </div>\n          </div>\n          <div class=\"recon-evidence-grid\">\n            <div class=\"recon-evidence-block statement\">\n              <span>Statement ต้นทาง</span>\n              <strong id=\"reconEvidenceFileCount\">0 ไฟล์</strong>\n              <div class=\"recon-evidence-files\" id=\"reconEvidenceFiles\"><small>ยังไม่มีไฟล์ต้นทาง</small></div>\n            </div>\n            <div class=\"recon-evidence-block\">\n              <span>ยืนยันล่าสุด</span>\n              <strong id=\"reconEvidenceActor\">—</strong>\n              <small id=\"reconEvidenceMatchedAt\">ยังไม่มีการยืนยันคู่</small>\n            </div>\n            <div class=\"recon-evidence-block\">\n              <span>ส่วนต่างที่ยืนยันแล้ว</span>\n              <strong id=\"reconEvidenceVariance\">฿0.00</strong>\n              <small>รวมค่าสัมบูรณ์ของส่วนต่างรายการที่จับคู่</small>\n            </div>\n            <div class=\"recon-evidence-block\">\n              <span>รายการค้างที่ยังไม่มีเหตุผล</span>\n              <strong id=\"reconEvidenceMissingReason\">0</strong>\n              <small>ควรใส่เหตุผลก่อนส่งตรวจหรือปิดรอบ</small>\n            </div>\n          </div>\n          <div class=\"recon-evidence-note\">\n            <span>หลักฐานที่ระบบเก็บ</span>\n            <span>Statement ต้นฉบับใน Google Drive · ผู้กระทบยอดและเวลา · ใบเบิก/สลิปที่จับคู่ · ส่วนต่าง · เหตุผลรายการที่ยังไม่ลงตัว</span>\n          </div>\n        </section>\n";
const RUNTIME="\n/* RUBJAI_RECON_EVIDENCE_DASHBOARD_V905_20260820 */\nfunction reconEvidenceRows(){\n  return Array.isArray(RECON_DATA.rows) ? RECON_DATA.rows : [];\n}\nfunction reconEvidenceFiles(){\n  const map=new Map();\n  for(const row of reconEvidenceRows()){\n    const name=String(row.sourceFile||\"\").trim();\n    if(!name)continue;\n    const url=String(row.sourceFileUrl||\"\").trim();\n    const key=url||name;\n    const prev=map.get(key);\n    const imported=String(row.importedAt||\"\");\n    if(!prev||imported>String(prev.importedAt||\"\"))map.set(key,{name,url,importedAt:imported});\n  }\n  return [...map.values()].sort((a,b)=>String(b.importedAt||\"\").localeCompare(String(a.importedAt||\"\")));\n}\nfunction reconEvidenceStats(){\n  const rows=reconEvidenceRows(), summary=RECON_DATA.summary||{};\n  const matched=rows.filter(r=>String(r.displayStatus||r.status||\"\")===\"กระทบยอดแล้ว\");\n  const pending=rows.filter(r=>[\"แนะนำอัตโนมัติ\",\"ต้องตรวจ\",\"ไม่พบคู่\"].includes(String(r.displayStatus||\"\")));\n  const ignored=rows.filter(r=>String(r.displayStatus||r.status||\"\")===\"ข้าม\");\n  const needsReason=[...pending,...ignored].filter(r=>!String(r.note||\"\").trim());\n  const last=matched.slice().sort((a,b)=>String(b.matchedAt||\"\").localeCompare(String(a.matchedAt||\"\")))[0]||null;\n  const variance=matched.reduce((sum,row)=>{\n    const b=row.linkedBatch||{};\n    const diff=Number(row.amount||0)-Number(b.total||b.amount||0);\n    return sum+(Number.isFinite(diff)?Math.abs(diff):0);\n  },0);\n  const waiting=Number(summary.unreconciledPaidBatches?.count||0);\n  const unresolved=pending.length+waiting;\n  return {rows,matched,pending,ignored,needsReason,last,variance,waiting,unresolved};\n}\nfunction reconDateTime(value){\n  if(!value)return \"—\";\n  const d=new Date(value);\n  if(Number.isNaN(d.getTime()))return reconDate(value);\n  try{return new Intl.DateTimeFormat(\"th-TH\",{dateStyle:\"medium\",timeStyle:\"short\",timeZone:\"Asia/Bangkok\"}).format(d);}\n  catch{return String(value);}\n}\nfunction renderReconEvidenceV905(){\n  if(!el(\"reconEvidenceCard\"))return;\n  const files=reconEvidenceFiles(), stats=reconEvidenceStats();\n  const hasRows=stats.rows.length>0;\n  const isComplete=hasRows&&stats.unresolved===0&&stats.needsReason.length===0;\n  const state=el(\"reconEvidenceState\");\n  state.textContent=!hasRows?\"ยังไม่มี Statement\":isComplete?\"หลักฐานครบสำหรับรอบนี้\":`ยังมีงานค้าง ${stats.unresolved} รายการ`;\n  state.className=\"recon-evidence-state \"+(!hasRows?\"empty\":isComplete?\"complete\":\"pending\");\n  el(\"reconEvidenceFileCount\").textContent=`${files.length.toLocaleString(\"th-TH\")} ไฟล์`;\n  el(\"reconEvidenceFiles\").innerHTML=files.length?files.slice(0,5).map(f=>{\n    const meta=f.importedAt?`<small>${reconDateTime(f.importedAt)}</small>`:\"\";\n    return f.url\n      ? `<a class=\"recon-file-chip\" href=\"${escAttr(f.url)}\" target=\"_blank\" rel=\"noopener\"><b>${esc(f.name)}</b>${meta}<span>เปิดต้นฉบับ ↗</span></a>`\n      : `<div class=\"recon-file-chip legacy\"><b>${esc(f.name)}</b>${meta}<span>ไฟล์เก่าไม่มีลิงก์ต้นฉบับ</span></div>`;\n  }).join(\"\"):'<small>ยังไม่มีไฟล์ต้นทาง</small>';\n  el(\"reconEvidenceActor\").textContent=stats.last?.matchedBy||\"—\";\n  el(\"reconEvidenceMatchedAt\").textContent=stats.last?.matchedAt?reconDateTime(stats.last.matchedAt):\"ยังไม่มีการยืนยันคู่\";\n  el(\"reconEvidenceVariance\").textContent=baht(stats.variance);\n  el(\"reconEvidenceMissingReason\").textContent=stats.needsReason.length.toLocaleString(\"th-TH\");\n  if(el(\"reconEvidenceCsv\"))el(\"reconEvidenceCsv\").disabled=!hasRows;\n  if(el(\"reconEvidencePrint\"))el(\"reconEvidencePrint\").disabled=!hasRows;\n\n  document.querySelectorAll(\"#reconBody tr[data-recon-row]\").forEach(tr=>{\n    const row=stats.rows.find(x=>String(x.id)===String(tr.dataset.reconRow));\n    const detail=tr.querySelector(\".recon-detail\");\n    if(!row||!detail)return;\n    detail.querySelector(\".recon-row-note-v905\")?.remove();\n    if(String(row.note||\"\").trim()){\n      detail.insertAdjacentHTML(\"beforeend\",`<span class=\"recon-row-note-v905\">เหตุผล/หมายเหตุ: ${esc(row.note)}</span>`);\n    }\n  });\n}\nfunction exportReconEvidenceCsvV905(){\n  const selected=RECON_DATA.selectedChannel||{};\n  const rows=reconEvidenceRows();\n  if(!rows.length)return alert(\"ยังไม่มีข้อมูลกระทบยอดให้ส่งออก\");\n  downloadCsv(\n    `reconciliation-evidence-${new Date().toISOString().slice(0,10)}.csv`,\n    [\"บัญชี\",\"วันที่ธนาคาร\",\"รายละเอียด\",\"อ้างอิง\",\"เงินออก\",\"สถานะ\",\"เลขใบเบิก\",\"ผู้รับเงิน\",\"ยอดใบเบิก\",\"ส่วนต่าง\",\"Statement\",\"ลิงก์ Statement\",\"กระทบยอดเมื่อ\",\"ผู้กระทบยอด\",\"เหตุผล/หมายเหตุ\"],\n    rows.map(row=>{\n      const b=row.linkedBatch||row.suggestion?.best||{};\n      const diff=b&&Number.isFinite(Number(b.total||b.amount)) ? Number(row.amount||0)-Number(b.total||b.amount||0) : \"\";\n      return [\n        financeChannelTitle(selected),row.transactionDate,row.description,row.reference,row.amount,\n        row.displayStatus||row.status,b.docId||\"\",b.payerName||\"\",b.total||b.amount||\"\",diff,\n        row.sourceFile||\"\",row.sourceFileUrl||\"\",row.matchedAt||\"\",row.matchedBy||\"\",row.note||\"\"\n      ];\n    })\n  );\n}\nfunction printReconEvidenceReportV905(){\n  const selected=RECON_DATA.selectedChannel||{}, rows=reconEvidenceRows(), stats=reconEvidenceStats(), files=reconEvidenceFiles(), summary=RECON_DATA.summary||{};\n  if(!rows.length)return alert(\"ยังไม่มีข้อมูลกระทบยอดให้พิมพ์\");\n  const w=window.open(\"\",\"_blank\",\"noopener,noreferrer\");\n  if(!w)return alert(\"เบราว์เซอร์บล็อกหน้าพิมพ์ กรุณาอนุญาต Pop-up แล้วลองใหม่\");\n  const detailRows=rows.map(row=>{\n    const b=row.linkedBatch||{};\n    const diff=b&&Number.isFinite(Number(b.total||b.amount))?Number(row.amount||0)-Number(b.total||b.amount||0):null;\n    return `<tr>\n      <td>${esc(reconDate(row.transactionDate))}</td>\n      <td>${esc(row.description||\"—\")}<br><small>${esc(row.reference||\"\")}</small></td>\n      <td class=\"num\">${esc(baht(row.amount))}</td>\n      <td>${esc(row.displayStatus||row.status||\"—\")}</td>\n      <td>${esc(b.docId||\"—\")}<br><small>${esc(b.payerName||\"\")}</small></td>\n      <td class=\"num\">${diff==null?\"—\":esc((diff>0?\"+\":\"\")+baht(diff).replace(\"฿\",\"\"))}</td>\n      <td>${esc(row.matchedBy||\"—\")}<br><small>${esc(row.matchedAt?reconDateTime(row.matchedAt):\"\")}</small></td>\n      <td>${esc(row.note||\"—\")}</td>\n    </tr>`;\n  }).join(\"\");\n  const filesHtml=files.length?files.map(f=>`<li>${esc(f.name)}${f.importedAt?` · ${esc(reconDateTime(f.importedAt))}`:\"\"}${f.url?` · <a href=\"${escAttr(f.url)}\">Drive</a>`:\"\"}</li>`).join(\"\"):\"<li>ไม่มีข้อมูลไฟล์ต้นฉบับ</li>\";\n  const reportHtml=`<!doctype html><html lang=\"th\"><head><meta charset=\"utf-8\"><title>รายงานกระทบยอด</title><style>\n    body{font-family:Arial,\"Noto Sans Thai\",sans-serif;color:#11162E;margin:28px;font-size:12px}\n    h1{font-size:22px;margin:0 0 4px}h2{font-size:15px;margin:24px 0 8px}.muted,small{color:#667085}\n    .grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:16px 0}\n    .box{border:1px solid #D9DEEA;border-radius:10px;padding:10px}.box span{display:block;color:#667085;font-size:10px}.box b{font-size:16px}\n    table{width:100%;border-collapse:collapse;margin-top:8px}th,td{border-bottom:1px solid #E4E7EC;padding:7px 5px;text-align:left;vertical-align:top}th{font-size:10px;color:#667085}.num{text-align:right}\n    ul{padding-left:18px}a{color:#11162E}@media print{body{margin:10mm}}\n  </style></head><body>\n    <h1>รายงานหลักฐานการกระทบยอด</h1>\n    <div class=\"muted\">${esc(financeChannelTitle(selected))} · สร้างรายงาน ${esc(reconDateTime(new Date().toISOString()))}</div>\n    <div class=\"grid\">\n      <div class=\"box\"><span>Statement</span><b>${rows.length.toLocaleString(\"th-TH\")} รายการ</b></div>\n      <div class=\"box\"><span>กระทบยอดแล้ว</span><b>${Number(summary.reconciled?.count||0).toLocaleString(\"th-TH\")}</b></div>\n      <div class=\"box\"><span>รอกระทบยอด</span><b>${Number(summary.unreconciledPaidBatches?.count||0).toLocaleString(\"th-TH\")}</b></div>\n      <div class=\"box\"><span>ส่วนต่างที่ยืนยัน</span><b>${esc(baht(stats.variance))}</b></div>\n    </div>\n    <h2>Statement ต้นทาง</h2><ul>${filesHtml}</ul>\n    <h2>ผู้ตรวจล่าสุด</h2><p>${esc(stats.last?.matchedBy||\"—\")} · ${esc(stats.last?.matchedAt?reconDateTime(stats.last.matchedAt):\"ยังไม่มีการยืนยัน\")}</p>\n    <h2>รายละเอียดรายการ</h2>\n    <table><thead><tr><th>วันที่</th><th>รายละเอียด</th><th class=\"num\">เงินออก</th><th>สถานะ</th><th>ใบเบิก</th><th class=\"num\">ส่วนต่าง</th><th>ผู้ตรวจ/เวลา</th><th>เหตุผล</th></tr></thead><tbody>${detailRows}</tbody></table>\n    <script>window.onload=()=>window.print();<\\/script>\n  </body></html>`;\n  w.document.open();w.document.write(reportHtml);w.document.close();\n}\nasync function saveReconNoteV905(id,note){\n  const clean=String(note||\"\").trim();\n  try{\n    const out=await reconciliationPost(\"/api/reconciliation-note\",{reconciliationId:id,note:clean});\n    const row=(RECON_DATA.rows||[]).find(x=>String(x.id)===String(id));\n    if(row)row.note=out.note??clean;\n    renderReconciliation();\n    if(ACTIVE_RECON_ID)renderReconDrawer(ACTIVE_RECON_ID);\n    return true;\n  }catch(err){\n    alert(\"บันทึกเหตุผลไม่สำเร็จ: \"+err.message);\n    return false;\n  }\n}\nconst renderReconciliationV905Base=renderReconciliation;\nrenderReconciliation=function(...args){\n  const result=renderReconciliationV905Base.apply(this,args);\n  renderReconEvidenceV905();\n  return result;\n};\nconst renderReconDrawerV905Base=renderReconDrawer;\nrenderReconDrawer=function(id,query=\"\"){\n  const result=renderReconDrawerV905Base.call(this,id,query);\n  const row=(RECON_DATA.rows||[]).find(x=>String(x.id)===String(id));\n  const body=el(\"reconDrawerBody\");\n  if(!row||!body)return result;\n  body.querySelector(\".recon-note-editor-v905\")?.remove();\n  body.insertAdjacentHTML(\"beforeend\",`\n    <div class=\"drawer-section recon-note-editor-v905\">\n      <h4>เหตุผล / หมายเหตุสำหรับตรวจย้อนหลัง</h4>\n      <p class=\"master-secondary\">ถ้ายังไม่พบคู่หรือต้องข้ามรายการ ให้ระบุเหตุผลไว้ เช่น ค่าธรรมเนียมธนาคาร, ยังไม่ได้บันทึกใบเบิก, ยอดต่างจากค่าธรรมเนียม หรือรอตรวจเอกสาร</p>\n      <textarea id=\"reconNoteV905\" rows=\"3\" placeholder=\"ระบุเหตุผลหรือหมายเหตุ…\">${esc(row.note||\"\")}</textarea>\n      <div class=\"recon-note-actions-v905\">\n        <button class=\"btn\" type=\"button\" data-recon-save-note=\"${escAttr(row.id)}\">บันทึกเหตุผล</button>\n        ${row.sourceFileUrl?`<a class=\"btn\" href=\"${escAttr(row.sourceFileUrl)}\" target=\"_blank\" rel=\"noopener\">เปิด Statement ต้นฉบับ</a>`:\"\"}\n      </div>\n    </div>\n  `);\n  return result;\n};\ndocument.addEventListener(\"click\",async e=>{\n  const save=e.target.closest(\"[data-recon-save-note]\");\n  if(save){\n    e.preventDefault();e.stopPropagation();\n    const btn=save,old=btn.textContent;btn.disabled=true;btn.textContent=\"กำลังบันทึก…\";\n    try{await saveReconNoteV905(save.dataset.reconSaveNote,el(\"reconNoteV905\")?.value||\"\");}\n    finally{btn.disabled=false;btn.textContent=old;}\n    return;\n  }\n  if(e.target.closest(\"#reconEvidenceCsv\")){e.preventDefault();exportReconEvidenceCsvV905();return;}\n  if(e.target.closest(\"#reconEvidencePrint\")){e.preventDefault();printReconEvidenceReportV905();return;}\n});\ndocument.addEventListener(\"click\",async e=>{\n  const ignore=e.target.closest(\"[data-recon-ignore]\");\n  if(!ignore)return;\n  e.preventDefault();e.stopImmediatePropagation();\n  const row=(RECON_DATA.rows||[]).find(x=>String(x.id)===String(ignore.dataset.reconIgnore));\n  const reason=prompt(\"ระบุเหตุผลที่ข้ามรายการนี้ เช่น ค่าธรรมเนียมธนาคาร / ไม่เกี่ยวกับใบเบิก\",String(row?.note||\"\"));\n  if(reason===null)return;\n  if(!String(reason).trim())return alert(\"กรุณาระบุเหตุผลก่อนข้ามรายการ\");\n  try{\n    await reconciliationPost(\"/api/reconciliation-ignore\",{reconciliationId:ignore.dataset.reconIgnore,note:String(reason).trim()});\n    closeReconDrawer();\n    await refreshReconciliation({quiet:true});\n  }catch(err){alert(\"ข้ามรายการไม่สำเร็จ: \"+err.message);}\n},true);\n";
const STYLE="\n/* RUBJAI_RECON_EVIDENCE_DASHBOARD_V905_20260820 */\n.recon-evidence-card{border:1px solid var(--line,#E4E7EC);background:#fff;border-radius:18px;padding:16px;margin:14px 0;box-shadow:0 1px 3px rgba(17,22,46,.025)}\n.recon-evidence-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin-bottom:14px}\n.recon-evidence-head h3{margin:3px 0 4px;font-size:17px;color:#11162E}.recon-evidence-head p{margin:0;color:#667085;font-size:11px;line-height:1.45}\n.recon-evidence-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap;justify-content:flex-end}\n.recon-evidence-state{display:inline-flex;align-items:center;min-height:31px;padding:6px 10px;border-radius:999px;background:#F0F2F8;color:#39405A;font-size:10px;font-weight:800;border:1px solid #D9DEEA}\n.recon-evidence-state.complete{background:#11162E;color:#fff;border-color:#11162E}.recon-evidence-state.pending{background:#F6F7FA;color:#11162E;border-color:#D9DEEA}.recon-evidence-state.empty{color:#667085}\n.recon-evidence-grid{display:grid;grid-template-columns:1.35fr repeat(3,1fr);gap:9px}.recon-evidence-block{background:#F7F8FB;border:1px solid #E4E7EC;border-radius:14px;padding:12px;min-width:0}\n.recon-evidence-block>span{display:block;color:#667085;font-size:9.5px;font-weight:700;margin-bottom:5px}.recon-evidence-block>strong{display:block;color:#11162E;font-size:17px;line-height:1.1}\n.recon-evidence-block>small{display:block;color:#98A2B3;font-size:9px;line-height:1.35;margin-top:5px}.recon-evidence-files{display:grid;gap:5px;margin-top:8px}\n.recon-file-chip{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:2px 8px;align-items:center;padding:7px 8px;border-radius:10px;background:#fff;border:1px solid #E4E7EC;color:#11162E;text-decoration:none}\n.recon-file-chip b{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:10px}.recon-file-chip small{grid-column:1;font-size:8.5px;color:#98A2B3}.recon-file-chip span{grid-column:2;grid-row:1/3;font-size:8.5px;font-weight:800;color:#5F6885}.recon-file-chip.legacy span{color:#98A2B3}\n.recon-evidence-note{display:flex;gap:9px;align-items:flex-start;margin-top:10px;padding-top:10px;border-top:1px solid #E4E7EC;color:#667085;font-size:9.5px;line-height:1.45}.recon-evidence-note>span:first-child{font-weight:800;color:#11162E;white-space:nowrap}\n.recon-row-note-v905{display:block!important;margin-top:4px!important;color:#5F6885!important;font-size:9px!important}\n.recon-note-editor-v905 textarea{width:100%;box-sizing:border-box;border:1px solid #D9DEEA;border-radius:12px;background:#fff;color:#11162E;padding:10px 11px;resize:vertical;font:inherit;line-height:1.45;outline:none}\n.recon-note-editor-v905 textarea:focus{border-color:#11162E;box-shadow:0 0 0 3px rgba(17,22,46,.08)}.recon-note-actions-v905{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}\n@media(max-width:900px){.recon-evidence-grid{grid-template-columns:1fr 1fr}.recon-evidence-block.statement{grid-column:1/-1}}\n@media(max-width:620px){.recon-evidence-card{padding:13px;border-radius:16px}.recon-evidence-head{display:block}.recon-evidence-actions{justify-content:flex-start;margin-top:10px}.recon-evidence-grid{grid-template-columns:1fr 1fr;gap:7px}.recon-evidence-block{padding:10px}.recon-evidence-block>strong{font-size:15px}.recon-evidence-note{display:block}.recon-evidence-note>span{display:block}.recon-evidence-note>span+span{margin-top:3px}}\n";
if(!html.includes('id="reconEvidenceCard"')){
  const anchor='        <div class="recon-table-card">';
  if(!html.includes(anchor))throw new Error("v9.05 recon table anchor not found");
  html=html.replace(anchor,CARD+"\n"+anchor);
}
if(!js.includes(MARK)){
  const boot="\nload();";
  if(!js.includes(boot))throw new Error("v9.05 load anchor not found");
  js=js.replace(boot,"\n"+RUNTIME+"\n"+boot);
}
if(!css.includes(MARK))css+="\n"+STYLE+"\n";
fs.writeFileSync(indexFile,html,"utf8");fs.writeFileSync(jsFile,js,"utf8");fs.writeFileSync(cssFile,css,"utf8");
execFileSync(process.execPath,["--check",jsFile],{stdio:"pipe"});
const checks=[html.includes('id="reconEvidenceCard"'),js.includes("/api/reconciliation-note"),js.includes("printReconEvidenceReportV905"),css.includes(MARK)];
if(checks.some(v=>!v))throw new Error("v9.05 audit failed");
console.log("✅ V9.05 reconciliation evidence UI");
console.log("✅ Statement source / actor / time / variance / reason");
console.log("✅ CSV + Print/PDF report");
