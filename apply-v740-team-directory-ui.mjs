import fs from "node:fs";
import path from "node:path";
import { execFileSync } from "node:child_process";

const root=process.cwd();
const indexFile=path.join(root,"index.html");
const assets=path.join(root,"assets");
const assetFile=path.join(assets,"team-directory-v740.js");
const MARK="TEAM_DIRECTORY_UI_V7_40_20260814";

if(!fs.existsSync(indexFile)) throw new Error("ไม่พบ index.html — ให้รันที่ root ของ deal-dashboard");
if(!fs.existsSync(assets)) fs.mkdirSync(assets,{recursive:true});

fs.writeFileSync(assetFile,"(()=>{\n  \"use strict\";\n  const MARK=\"TEAM_DIRECTORY_UI_V7_40_20260814\";\n  const $=(s,r=document)=>r.querySelector(s);\n  const $$=(s,r=document)=>[...r.querySelectorAll(s)];\n  let DIRECTORY={rows:[],groups:[],ready:0,waiting:0,total:0};\n  let loading=false;\n\n  function css(){\n    if($(\"#teamDirectoryV740Style\")) return;\n    const s=document.createElement(\"style\");\n    s.id=\"teamDirectoryV740Style\";\n    s.textContent=`\n      #biz-team .biz-grid{grid-template-columns:1fr!important}\n      #biz-team .team-directory-card-v740{order:-2}\n      #biz-team .team-manual-card-v740{order:2}\n      #biz-team .team-manual-card-v740 .biz-form{display:none}\n      #biz-team .team-manual-card-v740.manual-open .biz-form{display:block}\n      .team-toolbar-v740{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:14px 0}\n      .team-toolbar-v740 select{min-width:220px;height:42px;border:1px solid #e5e5ea;border-radius:12px;background:#fff;padding:0 10px}\n      .team-summary-v740{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:12px 0}\n      .team-summary-v740>div{background:#f5f5f7;border-radius:13px;padding:10px 12px}\n      .team-summary-v740 span{display:block;font-size:10px;color:#6e6e73}\n      .team-summary-v740 strong{display:block;font-size:18px;margin-top:2px}\n      .team-row-v740{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(130px,.7fr) auto;gap:12px;align-items:center;padding:13px 0;border-bottom:1px solid #eee}\n      .team-row-v740:last-child{border-bottom:0}\n      .team-person-v740{display:flex;gap:10px;align-items:center;min-width:0}\n      .team-avatar-v740{width:38px;height:38px;border-radius:50%;background:#f2f2f7;display:grid;place-items:center;overflow:hidden;flex:0 0 auto;font-weight:800}\n      .team-avatar-v740 img{width:100%;height:100%;object-fit:cover}\n      .team-person-v740 strong{font-size:12px;display:block}\n      .team-person-v740 small{font-size:10px;color:#6e6e73;display:block;margin-top:3px;line-height:1.45}\n      .team-state-v740{font-size:10px;font-weight:800;border-radius:999px;padding:6px 9px;display:inline-block}\n      .team-state-v740.ready{background:#edf8ef;color:#248a3d}\n      .team-state-v740.wait{background:#fff6e8;color:#9a5a00}\n      .team-actions-v740{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap}\n      .team-empty-v740{padding:24px;text-align:center;color:#6e6e73;font-size:12px}\n      .team-hint-v740{background:#f5f5f7;border-radius:14px;padding:11px 13px;font-size:11px;color:#6e6e73;line-height:1.55;margin:10px 0}\n      .team-modal-grid-v740{display:grid;grid-template-columns:1fr 1fr;gap:10px}\n      .team-modal-grid-v740 .full{grid-column:1/-1}\n      .team-modal-grid-v740 label{font-size:11px;font-weight:700;display:block;margin-bottom:5px}\n      .team-modal-grid-v740 input,.team-modal-grid-v740 select{width:100%;height:44px;border:1px solid #e5e5ea;border-radius:12px;padding:0 11px}\n      @media(max-width:760px){\n        .team-row-v740{grid-template-columns:1fr}\n        .team-actions-v740{justify-content:flex-start}\n        .team-summary-v740{grid-template-columns:1fr 1fr 1fr}\n        .team-toolbar-v740>*{width:100%;min-width:0!important}\n        .team-modal-grid-v740{grid-template-columns:1fr}\n        .team-modal-grid-v740 .full{grid-column:auto}\n      }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function api(path){ return `${WORKER}${path}?tenant=${encodeURIComponent(TENANT)}&k=${encodeURIComponent(K)}`; }\n  function maskAccount(v){\n    const d=String(v||\"\").replace(/\\D/g,\"\");\n    if(!d) return \"\";\n    return d.length<=4 ? \"••••\"+d : \"••••\"+d.slice(-4);\n  }\n  function escv(v){ return typeof esc===\"function\"?esc(v):String(v??\"\").replace(/[&<>\"]/g,m=>({\"&\":\"&amp;\",\"<\":\"&lt;\",\">\":\"&gt;\",'\"':\"&quot;\"}[m])); }\n\n  function setupStructure(){\n    css();\n    const tab=$(\"#biz-team\");\n    const grid=tab?.querySelector(\".biz-grid\");\n    const list=$(\"#teamList\");\n    if(!tab||!grid||!list) return false;\n\n    const cards=[...grid.children].filter(x=>x.classList?.contains(\"card\"));\n    const manual=cards.find(c=>c.querySelector(\"#teamName\"));\n    const directory=list.closest(\".card\");\n    if(manual) manual.classList.add(\"team-manual-card-v740\");\n    if(directory){\n      directory.classList.add(\"team-directory-card-v740\");\n      if(grid.firstElementChild!==directory) grid.insertBefore(directory,grid.firstElementChild);\n      const h=directory.querySelector(\"h3\");\n      const sub=directory.querySelector(\".cs\");\n      if(h) h.textContent=\"พนักงานจาก LINE\";\n      if(sub) sub.textContent=\"สมาชิกจะเข้าทีมอัตโนมัติเมื่อระบบพบใน LINE และสถานะพร้อมรับเงินจะขึ้นหลังเจ้าตัวลงทะเบียนข้อมูล\";\n    }\n\n    if(manual){\n      const h=manual.querySelector(\"h3\");\n      const sub=manual.querySelector(\".cs\");\n      if(h) h.textContent=\"เพิ่มหรือแก้ข้อมูลเอง\";\n      if(sub) sub.textContent=\"ใช้เป็นทางเลือกสำรอง กรณีพนักงานไม่สะดวกลงทะเบียนจาก LINE\";\n    }\n\n    if(directory&&!$(\"#teamToolbarV740\")){\n      const toolbar=document.createElement(\"div\");\n      toolbar.id=\"teamToolbarV740\";\n      toolbar.innerHTML=`\n        <div class=\"team-hint-v740\"><b style=\"color:#111\">วิธีที่แนะนำ:</b> เชื่อมกลุ่ม LINE → กด “ส่งคำเชิญในกลุ่ม” → พนักงานแต่ละคนกดลงทะเบียนของตัวเองครั้งเดียว</div>\n        <div class=\"team-summary-v740\">\n          <div><span>สมาชิกที่พบ</span><strong id=\"teamTotalV740\">0</strong></div>\n          <div><span>พร้อมรับเงิน</span><strong id=\"teamReadyV740\">0</strong></div>\n          <div><span>รอลงทะเบียน</span><strong id=\"teamWaitingV740\">0</strong></div>\n        </div>\n        <div class=\"team-toolbar-v740\">\n          <select id=\"teamGroupV740\"><option value=\"\">เลือกกลุ่ม LINE</option></select>\n          <button class=\"btn solid\" id=\"teamInviteGroupV740\" type=\"button\">ส่งคำเชิญลงทะเบียนในกลุ่ม</button>\n          <button class=\"btn\" id=\"teamRefreshV740\" type=\"button\">อัปเดตจาก LINE</button>\n          <button class=\"btn\" id=\"teamManualToggleV740\" type=\"button\">+ เพิ่มเอง</button>\n        </div>`;\n      directory.insertBefore(toolbar,list);\n      $(\"#teamInviteGroupV740\")?.addEventListener(\"click\",inviteGroup);\n      $(\"#teamRefreshV740\")?.addEventListener(\"click\",()=>loadDirectory(true));\n      $(\"#teamManualToggleV740\")?.addEventListener(\"click\",()=>{\n        manual?.classList.toggle(\"manual-open\");\n        if(manual?.classList.contains(\"manual-open\")) manual.scrollIntoView({behavior:\"smooth\",block:\"nearest\"});\n      });\n    }\n    return true;\n  }\n\n  function fillGroupSelect(){\n    const sel=$(\"#teamGroupV740\");\n    if(!sel) return;\n    const current=sel.value;\n    sel.innerHTML=`<option value=\"\">เลือกกลุ่ม LINE</option>`+(DIRECTORY.groups||[]).map(g=>`<option value=\"${escv(g.tenant)}\">${escv(g.name||\"กลุ่ม LINE\")} · ${Number(g.count||0)} คน</option>`).join(\"\");\n    if([...sel.options].some(o=>o.value===current)) sel.value=current;\n    else if(sel.options.length===2) sel.selectedIndex=1;\n  }\n\n  function renderRows(){\n    if(!setupStructure()) return;\n    $(\"#teamTotalV740\").textContent=String(DIRECTORY.total||0);\n    $(\"#teamReadyV740\").textContent=String(DIRECTORY.ready||0);\n    $(\"#teamWaitingV740\").textContent=String(DIRECTORY.waiting||0);\n    fillGroupSelect();\n    const list=$(\"#teamList\");\n    const rows=DIRECTORY.rows||[];\n    if(!rows.length){\n      list.innerHTML=`<div class=\"team-empty-v740\">ยังไม่พบสมาชิก LINE<br><br>ส่งคำเชิญในกลุ่ม หรือกด “อัปเดตจาก LINE” หลังเพิ่ม Bot เข้ากลุ่ม</div>`;\n      return;\n    }\n    list.innerHTML=rows.map((x,i)=>{\n      const name=x.name||x.displayName||\"สมาชิก LINE\";\n      const groups=(x.groups||[]).map(g=>g.name).filter(Boolean).join(\" · \");\n      const bank=[x.bank,maskAccount(x.accountNo)].filter(Boolean).join(\" \");\n      const status=x.profileComplete\n        ? `<span class=\"team-state-v740 ready\">พร้อมรับเงิน</span>`\n        : `<span class=\"team-state-v740 wait\">ยังไม่ลงทะเบียน</span>`;\n      const avatar=x.pictureUrl?`<img src=\"${escv(x.pictureUrl)}\" alt=\"\">`:escv(String(name).trim().slice(0,1)||\"•\");\n      return `<div class=\"team-row-v740\">\n        <div class=\"team-person-v740\"><div class=\"team-avatar-v740\">${avatar}</div><div><strong>${escv(name)}</strong><small>${escv(x.role||\"พนักงาน\")}${groups?` · ${escv(groups)}`:\"\"}<br>${bank?escv(bank):\"ยังไม่มีบัญชีรับเงิน\"}${x.lineUserId?` · LINE ···${escv(String(x.lineUserId).slice(-6))}`:\"\"}</small></div></div>\n        <div>${status}</div>\n        <div class=\"team-actions-v740\">\n          ${!x.profileComplete&&x.lineUserId?`<button class=\"btn small\" type=\"button\" data-team-v740-remind=\"${escv(x.lineUserId)}\">ส่งเตือน</button>`:\"\"}\n          <button class=\"btn small\" type=\"button\" data-team-v740-edit=\"${i}\">แก้ข้อมูล</button>\n        </div>\n      </div>`;\n    }).join(\"\");\n  }\n\n  async function loadDirectory(refresh=false){\n    if(loading) return;\n    loading=true;\n    const btn=$(\"#teamRefreshV740\");\n    const old=btn?.textContent;\n    if(btn){btn.disabled=true;btn.textContent=\"กำลังอ่าน LINE…\";}\n    try{\n      const res=await fetch(api(\"/api/team-directory\")+`&refresh=${refresh?1:0}&_=${Date.now()}`,{cache:\"no-store\"});\n      const data=await res.json().catch(()=>({}));\n      if(!res.ok||data.ok!==true) throw new Error(data.message||data.error||`HTTP ${res.status}`);\n      DIRECTORY=data;\n      renderRows();\n    }catch(e){\n      console.error(\"team directory\",e);\n      const list=$(\"#teamList\");\n      if(list) list.innerHTML=`<div class=\"team-empty-v740\">โหลดสมาชิกจาก LINE ไม่สำเร็จ · ${escv(e.message||e)}</div>`;\n    }finally{\n      loading=false;\n      if(btn){btn.disabled=false;btn.textContent=old||\"อัปเดตจาก LINE\";}\n    }\n  }\n\n  async function inviteGroup(){\n    const groupTenant=$(\"#teamGroupV740\")?.value||\"\";\n    if(!groupTenant) return alert(\"เลือกกลุ่ม LINE ก่อน\");\n    const b=$(\"#teamInviteGroupV740\"),old=b?.textContent;\n    if(b){b.disabled=true;b.textContent=\"กำลังส่ง…\";}\n    try{\n      const res=await fetch(api(\"/api/team-invite-group\"),{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({groupTenant})});\n      const data=await res.json().catch(()=>({}));\n      if(!res.ok||data.ok!==true) throw new Error(data.message||data.error||\"ส่งไม่สำเร็จ\");\n      alert(`ส่งการ์ดลงทะเบียนเข้า ${data.groupName||\"กลุ่ม LINE\"} แล้ว`);\n    }catch(e){alert(\"ส่งคำเชิญไม่สำเร็จ: \"+(e.message||e));}\n    finally{if(b){b.disabled=false;b.textContent=old||\"ส่งคำเชิญลงทะเบียนในกลุ่ม\";}}\n  }\n\n  async function remindUser(lineUserId){\n    try{\n      const res=await fetch(api(\"/api/team-invite-user\"),{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({lineUserId})});\n      const data=await res.json().catch(()=>({}));\n      if(!res.ok||data.ok!==true) throw new Error(data.message||data.error||\"ส่งไม่สำเร็จ\");\n      alert(\"ส่งลิงก์ลงทะเบียนไปแชทส่วนตัวแล้ว\");\n    }catch(e){alert((e.message||e)+\"\\n\\nถ้ายังส่งส่วนตัวไม่ได้ ให้พนักงานเพิ่ม LINE OA เป็นเพื่อน แล้วกดลงทะเบียนในกลุ่มอีกครั้ง\");}\n  }\n\n  function bankOptions(selected=\"\"){\n    const banks=[\"SCB\",\"KBANK\",\"KTB\",\"BBL\",\"BAY\",\"TTB\",\"GSB\",\"CIMB\",\"UOB\",\"KKP\",\"LH BANK\",\"อื่น ๆ\"];\n    return `<option value=\"\">เลือกธนาคาร</option>`+banks.map(b=>`<option ${String(selected).toUpperCase()===b?\"selected\":\"\"}>${b}</option>`).join(\"\");\n  }\n\n  function openEdit(rowIndex){\n    const row=DIRECTORY.rows?.[rowIndex];\n    if(!row) return;\n    const modal=$(\"#modal\"),body=$(\"#modalBody\");\n    if(!modal||!body) return;\n    body.innerHTML=`<h3 style=\"margin:0 0 5px\">แก้ข้อมูลพนักงาน</h3>\n      <p style=\"margin:0 0 16px;color:#6e6e73;font-size:12px\">แก้เฉพาะข้อมูลรับเงิน ไม่เปลี่ยนสิทธิ์ Dashboard</p>\n      <div class=\"team-modal-grid-v740\">\n        <div class=\"full\"><label>ชื่อ–นามสกุล</label><input id=\"teamModalNameV740\" value=\"${escv(row.name||row.displayName||\"\")}\"></div>\n        <div><label>ชื่อเล่น</label><input id=\"teamModalNickV740\" value=\"${escv(row.nickname||\"\")}\"></div>\n        <div><label>แผนก / บทบาท</label><input id=\"teamModalRoleV740\" value=\"${escv(row.role||\"พนักงาน\")}\"></div>\n        <div><label>ธนาคาร</label><select id=\"teamModalBankV740\">${bankOptions(row.bank||\"\")}</select></div>\n        <div><label>เลขบัญชี</label><input id=\"teamModalAccountV740\" inputmode=\"numeric\" value=\"${escv(row.accountNo||\"\")}\"></div>\n        <div class=\"full\"><label>ชื่อบัญชี</label><input id=\"teamModalAccountNameV740\" value=\"${escv(row.accountName||\"\")}\"></div>\n      </div>\n      <div style=\"display:flex;gap:8px;margin-top:16px;justify-content:flex-end\">\n        <button class=\"btn\" type=\"button\" id=\"teamModalCancelV740\">ยกเลิก</button>\n        <button class=\"btn solid\" type=\"button\" id=\"teamModalSaveV740\">บันทึก</button>\n      </div>`;\n    modal.classList.add(\"show\");\n    $(\"#teamModalCancelV740\")?.addEventListener(\"click\",()=>closeGlobalModal());\n    $(\"#teamModalSaveV740\")?.addEventListener(\"click\",()=>saveEdit(row));\n    setTimeout(()=>$(\"#teamModalNameV740\")?.focus(),40);\n  }\n\n  async function saveEdit(row){\n    const name=$(\"#teamModalNameV740\")?.value.trim()||\"\";\n    const bank=$(\"#teamModalBankV740\")?.value.trim()||\"\";\n    const accountNo=$(\"#teamModalAccountV740\")?.value.replace(/[^0-9-]/g,\"\").trim()||\"\";\n    const accountName=$(\"#teamModalAccountNameV740\")?.value.trim()||\"\";\n    if(!name) return alert(\"กรอกชื่อ–นามสกุล\");\n    const members=parseSettingList(\"team_members\").map(x=>({...x}));\n    let idx=Number.isInteger(row.savedIndex)&&row.savedIndex>=0?row.savedIndex:-1;\n    if(idx<0&&row.lineUserId) idx=members.findIndex(m=>String(m.lineUserId||m.payerId||\"\")===String(row.lineUserId));\n    const next={\n      ...(idx>=0?members[idx]:{}),\n      name,\n      nickname:$(\"#teamModalNickV740\")?.value.trim()||\"\",\n      role:$(\"#teamModalRoleV740\")?.value.trim()||\"พนักงาน\",\n      lineUserId:row.lineUserId||\"\",\n      bank,\n      accountNo,\n      accountName,\n      source:idx>=0?(members[idx]?.source||\"dashboard_edit\"):\"dashboard_edit\",\n      updatedAt:new Date().toISOString()\n    };\n    if(idx>=0) members[idx]=next; else members.push(next);\n    const ok=await saveSettings({team_members:JSON.stringify(members)},\"\");\n    if(!ok) return alert(\"บันทึกไม่สำเร็จ\");\n    closeGlobalModal();\n    await loadDirectory(false);\n  }\n\n  function bindClicks(){\n    document.addEventListener(\"click\",e=>{\n      const edit=e.target.closest(\"[data-team-v740-edit]\");\n      if(edit){e.preventDefault();e.stopImmediatePropagation();openEdit(Number(edit.dataset.teamV740Edit));return;}\n      const remind=e.target.closest(\"[data-team-v740-remind]\");\n      if(remind){e.preventDefault();e.stopImmediatePropagation();remindUser(remind.dataset.teamV740Remind);return;}\n      // กัน handler เก่าที่พา scroll ไปฟอร์มด้านบน\n      const legacy=e.target.closest(\"[data-edit-team],[data-prefill-team]\");\n      if(legacy){\n        e.preventDefault();e.stopImmediatePropagation();\n        const savedIndex=Number(legacy.dataset.editTeam);\n        let rowIndex=-1;\n        if(Number.isFinite(savedIndex)) rowIndex=(DIRECTORY.rows||[]).findIndex(x=>x.savedIndex===savedIndex);\n        if(rowIndex<0) rowIndex=0;\n        if(DIRECTORY.rows?.[rowIndex]) openEdit(rowIndex);\n      }\n    },true);\n  }\n\n  function hookRender(){\n    if(typeof renderBusiness!==\"function\"||renderBusiness.__v740) return;\n    const original=renderBusiness;\n    const wrapped=function(...args){\n      const out=original.apply(this,args);\n      try{\n        if($(\"#biz-team\")?.classList.contains(\"show\")){\n          setupStructure();\n          setTimeout(()=>loadDirectory(false),0);\n        }\n      }catch(e){console.warn(\"team v740 render\",e);}\n      return out;\n    };\n    wrapped.__v740=true;\n    renderBusiness=wrapped;\n  }\n\n  function init(){\n    css();bindClicks();hookRender();\n    if($(\"#biz-team\")?.classList.contains(\"show\")){\n      setupStructure();loadDirectory(false);\n    }\n  }\n  if(document.readyState===\"loading\") document.addEventListener(\"DOMContentLoaded\",init,{once:true});\n  else init();\n  window.addEventListener(\"load\",()=>setTimeout(init,100),{once:true});\n  console.info(MARK);\n})();");
execFileSync(process.execPath,["--check",assetFile],{stdio:"inherit"});

let html=fs.readFileSync(indexFile,"utf8");
if(!html.includes(MARK)){
  html=html.replace("</body>",`<!-- ${MARK} -->
<script src="./assets/team-directory-v740.js?v=7.40"></script>
</body>`);
  fs.writeFileSync(indexFile,html);
}
console.log("✅ "+MARK+" ready");
