import fs from "node:fs";
import path from "node:path";
import { execFileSync } from "node:child_process";

const root=process.cwd();
const file=path.join(root,"assets","dashboard.js");
const indexFile=path.join(root,"index.html");
const MARK="CONNECTION_INTERACTION_STATE_SAFETY_V7_61_20260816";

if(!fs.existsSync(file))throw new Error("ไม่พบ assets/dashboard.js");
let src=fs.readFileSync(file,"utf8");

if(src.includes(MARK)){
  console.log("ℹ️ "+MARK+" already present");
  process.exit(0);
}

for(const required of [
  "CONNECTION_STABILITY_V7_50_20260815",
  "PRODUCTION_AUTH_UI_GUARD_V7_52_20260815",
  "GOOGLE_HEALTH_PRIMARY_TRUTH_V7_54_20260815",
  "GMAIL_RETURN_OVERVIEW_V7_55_20260815"
]){
  if(!src.includes(required))throw new Error("ต้องมี "+required+" ก่อน v7.61");
}

// Transient workspace-link failure ห้ามล้าง href ที่เคยใช้ได้.
const destructiveLinks='    setWorkspaceLink("openSheetLink","","");setWorkspaceLink("openDriveLink","","");\n    return false;';
if(src.includes(destructiveLinks)){
  src=src.replace(
    destructiveLinks,
    '    // '+MARK+': transient GET failure must preserve last-known-good links.\n    return false;'
  );
}

const runtime="/* CONNECTION_INTERACTION_STATE_SAFETY_V7_61_20260816\n   หลักการ:\n   1) GET/status failure = UNKNOWN ไม่ใช่ DISCONNECTED\n   2) ปุ่มดูสถานะห้ามพา OAuth ถ้ายังพิสูจน์ไม่ได้ว่าหลุดจริง\n   3) เก็บ last-known-good ข้าม full-page navigation\n   4) transient refresh ห้ามล้าง Sheet/Drive links หรือเปลี่ยน UI เป็น \"เชื่อมใหม่\"\n*/\nconst V761_CONNECTION_LKG_KEY=`dashboard:connection-lkg:v761:${TENANT}`;\nlet V761_GMAIL_KNOWN=false;\nlet V761_GOOGLE_KNOWN=false;\nlet V761_GMAIL_PROBE_ERROR=false;\nlet V761_GOOGLE_PROBE_ERROR=false;\nlet V761_BUSINESS_PROBE_UNKNOWN=true;\nlet V761_WORKSPACE_PROBE_UNKNOWN=true;\n\nfunction v761ReadLkg(){\n  try{\n    const raw=localStorage.getItem(V761_CONNECTION_LKG_KEY);\n    const data=raw?JSON.parse(raw):{};\n    return data&&typeof data===\"object\"?data:{};\n  }catch{return {};}\n}\nfunction v761WriteLkg(patch={}){\n  try{\n    const current=v761ReadLkg();\n    localStorage.setItem(V761_CONNECTION_LKG_KEY,JSON.stringify({...current,...patch,updatedAt:Date.now()}));\n  }catch{}\n}\nfunction v761WriteGmailLkg(){\n  v761WriteLkg({gmail:{\n    known:V761_GMAIL_KNOWN===true,\n    connected:EMAIL_INFO?.connected===true,\n    reconnectRequired:EMAIL_INFO?.reconnectRequired===true||EMAIL_INFO?.statusReason===\"reconnect_required\",\n    everConnected:EMAIL_INFO?.everConnected===true,\n    email:String(EMAIL_INFO?.email||\"\"),\n    statusReason:String(EMAIL_INFO?.statusReason||\"\"),\n    at:Date.now()\n  }});\n}\nfunction v761WriteGoogleLkg(){\n  v761WriteLkg({google:{\n    known:V761_GOOGLE_KNOWN===true,\n    connected:GOOGLE_CORE_INFO?.connected===true,\n    reconnectRequired:GOOGLE_CORE_INFO?.reconnectRequired===true,\n    reason:String(GOOGLE_CORE_INFO?.reason||\"\"),\n    at:Date.now()\n  }});\n}\n\n(function v761SeedLastKnownGood(){\n  const lkg=v761ReadLkg();\n  const g=lkg.gmail;\n  if(g?.known===true){\n    V761_GMAIL_KNOWN=true;\n    EMAIL_INFO={\n      ...EMAIL_INFO,\n      connected:g.connected===true,\n      reconnectRequired:g.reconnectRequired===true,\n      everConnected:g.everConnected===true,\n      email:g.email||EMAIL_INFO?.email||\"\",\n      statusReason:g.statusReason||(g.reconnectRequired?\"reconnect_required\":g.connected?\"connected\":\"never_connected\")\n    };\n  }\n  const core=lkg.google;\n  if(core?.known===true){\n    V761_GOOGLE_KNOWN=true;\n    GOOGLE_CORE_INFO={\n      ...GOOGLE_CORE_INFO,\n      connected:core.connected===true,\n      reconnectRequired:core.reconnectRequired===true,\n      reason:core.reason||(core.reconnectRequired?\"reconnect_required\":core.connected?\"connected\":\"never_connected\")\n    };\n  }\n})();\n\nfunction v761CaptureExplicitTruth(){\n  if(EMAIL_INFO?.reconnectRequired===true||EMAIL_INFO?.statusReason===\"reconnect_required\"){\n    V761_GMAIL_KNOWN=true;\n    EMAIL_INFO={...EMAIL_INFO,connected:false,reconnectRequired:true,statusReason:\"reconnect_required\",everConnected:true};\n    v761WriteGmailLkg();\n  }else if(EMAIL_INFO?.connected===true){\n    V761_GMAIL_KNOWN=true;\n    v761WriteGmailLkg();\n  }\n  if(GOOGLE_CORE_INFO?.reconnectRequired===true){\n    V761_GOOGLE_KNOWN=true;\n    GOOGLE_CORE_INFO={...GOOGLE_CORE_INFO,connected:false,reconnectRequired:true};\n    v761WriteGoogleLkg();\n  }else if(GOOGLE_CORE_INFO?.connected===true){\n    V761_GOOGLE_KNOWN=true;\n    v761WriteGoogleLkg();\n  }\n}\n\nfunction v761GmailState(){\n  v761CaptureExplicitTruth();\n  if(EMAIL_INFO?.reconnectRequired===true||EMAIL_INFO?.statusReason===\"reconnect_required\")return \"reconnect\";\n  if(EMAIL_INFO?.connected===true)return \"connected\";\n  if(V761_GMAIL_KNOWN===true)return \"never\";\n  return \"unknown\";\n}\nfunction v761GoogleState(){\n  v761CaptureExplicitTruth();\n  if(GOOGLE_CORE_INFO?.reconnectRequired===true)return \"reconnect\";\n  if(GOOGLE_CORE_INFO?.connected===true)return \"connected\";\n  if(V761_GOOGLE_KNOWN===true)return \"never\";\n  return \"unknown\";\n}\n\nconst v761RefreshGmailConnectionStatusCore=refreshGmailConnectionStatus;\nrefreshGmailConnectionStatus=async function({retries=1,delayMs=350}={}){\n  let lastError=null;\n  const count=Math.max(1,Number(retries)||1);\n  for(let attempt=0;attempt<count;attempt++){\n    try{\n      const res=await fetch(apiUrl(\"/api/gmail-status\")+`&_=${Date.now()}`,{\n        cache:\"no-store\",\n        headers:{\"cache-control\":\"no-cache\"}\n      });\n      const data=await res.json().catch(()=>null);\n      if(!res.ok)throw new Error(data?.message||data?.error||`Gmail status ${res.status}`);\n      if(!data||typeof data.connected!==\"boolean\"||typeof data.reconnectRequired!==\"boolean\"){\n        throw new Error(\"invalid_gmail_status_payload\");\n      }\n      EMAIL_INFO={...EMAIL_INFO,...data};\n      V761_GMAIL_KNOWN=true;\n      V761_GMAIL_PROBE_ERROR=false;\n      v761WriteGmailLkg();\n\n      const page=currentPageKey();\n      if(page===\"email\")renderEmailInbox();\n      if(page===\"settings\")renderSettings();\n      renderOnboarding();\n      return data.connected===true&&data.reconnectRequired!==true;\n    }catch(err){\n      lastError=err;\n      console.warn(\"gmail status refresh transient\",err);\n    }\n    if(attempt<count-1)await new Promise(resolve=>setTimeout(resolve,delayMs));\n  }\n\n  V761_GMAIL_PROBE_ERROR=true;\n  // สำคัญ: ห้ามแก้ EMAIL_INFO เป็น false จาก timeout/500/network\n  // ถ้ามี LKG ให้ใช้ต่อ; ถ้าไม่มีให้เป็น unknown\n  const state=v761GmailState();\n  if(lastError)console.warn(\"gmail status unavailable; preserving previous truth\",lastError);\n  if(currentPageKey()===\"email\")renderEmailInbox();\n  if(currentPageKey()===\"settings\")renderSettings();\n  renderOnboarding();\n  return state===\"connected\";\n};\n\nconst v761RefreshGoogleCoreStatusCore=refreshGoogleCoreStatus;\nrefreshGoogleCoreStatus=async function(){\n  try{\n    const res=await fetch(apiUrl(\"/api/google-status\")+`&_=${Date.now()}`,{\n      cache:\"no-store\",\n      headers:{\"cache-control\":\"no-cache\"}\n    });\n    const data=await res.json().catch(()=>null);\n    if(!res.ok||!data)throw new Error(data?.message||data?.error||`Google status ${res.status}`);\n    if(typeof data.connected!==\"boolean\"||typeof data.reconnectRequired!==\"boolean\"){\n      throw new Error(\"invalid_google_status_payload\");\n    }\n    GOOGLE_CORE_INFO={...GOOGLE_CORE_INFO,...data};\n    V761_GOOGLE_KNOWN=true;\n    V761_GOOGLE_PROBE_ERROR=false;\n    v761WriteGoogleLkg();\n    return data.connected===true&&data.reconnectRequired!==true;\n  }catch(err){\n    console.warn(\"google core status transient\",err);\n    V761_GOOGLE_PROBE_ERROR=true;\n\n    // ถ้าระบบเคยอ่านข้อมูลจริงได้ ห้าม status probe ครั้งเดียวล้มความจริงนั้น\n    if(GOOGLE_CORE_INFO?.reconnectRequired===true)return false;\n    if(CONNECTED||coreReadyGraceActive()||v761GoogleState()===\"connected\"){\n      GOOGLE_CORE_INFO={\n        ...GOOGLE_CORE_INFO,\n        connected:true,\n        reconnectRequired:false,\n        reason:\"last_known_good\",\n        detail:\"\"\n      };\n      V761_GOOGLE_KNOWN=true;\n      v761WriteGoogleLkg();\n      return true;\n    }\n    return false;\n  }\n};\n\nconst v761RefreshBusinessesCore=refreshBusinesses;\nrefreshBusinesses=async function(...args){\n  const ok=await v761RefreshBusinessesCore.apply(this,args);\n  V761_BUSINESS_PROBE_UNKNOWN=ok!==true;\n  if(ok===true){\n    const ready=currentBusinessConnected();\n    v761WriteLkg({business:{known:true,ready,at:Date.now()}});\n  }\n  return ok;\n};\n\nconst v761RefreshWorkspaceLinksCore=refreshWorkspaceLinks;\nrefreshWorkspaceLinks=async function(...args){\n  const before={...WORKSPACE_LINKS};\n  const oldSheet=el(\"openSheetLink\")?.getAttribute(\"href\")||\"\";\n  const oldDrive=el(\"openDriveLink\")?.getAttribute(\"href\")||\"\";\n  const ok=await v761RefreshWorkspaceLinksCore.apply(this,args);\n  V761_WORKSPACE_PROBE_UNKNOWN=ok!==true;\n  if(ok===true){\n    v761WriteLkg({workspace:{known:true,ready:true,sheetUrl:WORKSPACE_LINKS.sheetUrl||\"\",driveUrl:WORKSPACE_LINKS.driveUrl||\"\",at:Date.now()}});\n    return true;\n  }\n\n  // GET fail ห้ามทำลิงก์ที่เคยใช้ได้หาย\n  const lkg=v761ReadLkg().workspace||{};\n  const sheet=before.sheetUrl||((oldSheet&&oldSheet!==\"#\")?oldSheet:\"\")||lkg.sheetUrl||\"\";\n  const drive=before.driveUrl||((oldDrive&&oldDrive!==\"#\")?oldDrive:\"\")||lkg.driveUrl||\"\";\n  if(sheet||drive){\n    WORKSPACE_LINKS={sheetUrl:sheet,driveUrl:drive};\n    setWorkspaceLink(\"openSheetLink\",sheet,\"เปิด Google Sheet ของบริษัทในแท็บใหม่\");\n    setWorkspaceLink(\"openDriveLink\",drive,\"เปิด Google Drive ที่เก็บเอกสารในแท็บใหม่\");\n  }\n  return false;\n};\n\nfunction v761ConnectionSnapshot(){\n  const lkg=v761ReadLkg();\n  const gstate=v761GoogleState();\n  const mstate=v761GmailState();\n\n  const priorBusiness=CONNECTION_HEALTH?.business===true||lkg.business?.ready===true||coreReadyGraceActive();\n  const priorWorkspace=CONNECTION_HEALTH?.workspace===true||lkg.workspace?.ready===true||coreReadyGraceActive();\n\n  const businessUnknown=V761_BUSINESS_PROBE_UNKNOWN===true;\n  const workspaceUnknown=V761_WORKSPACE_PROBE_UNKNOWN===true;\n\n  const businessReady=businessUnknown?priorBusiness:currentBusinessConnected();\n  const workspaceReady=workspaceUnknown?priorWorkspace:true;\n\n  return {\n    googleState:gstate,\n    gmailState:mstate,\n    businessReady,\n    workspaceReady,\n    businessUnknown,\n    workspaceUnknown\n  };\n}\n\nrenderConnectionHealthBanner=function(){\n  const box=ensureConnectionHealthBanner();\n  if(!box)return;\n\n  v761CaptureExplicitTruth();\n  const snap=v761ConnectionSnapshot();\n  const googleUnknown=snap.googleState===\"unknown\";\n  const gmailUnknown=snap.gmailState===\"unknown\";\n\n  const confirmedCoreDisconnect=\n    snap.googleState===\"reconnect\"||\n    snap.googleState===\"never\"||\n    (!snap.businessReady&&!snap.businessUnknown)||\n    (!snap.workspaceReady&&!snap.workspaceUnknown);\n\n  const coreUnknown=!confirmedCoreDisconnect&&(\n    googleUnknown||\n    (!snap.businessReady&&snap.businessUnknown)||\n    (!snap.workspaceReady&&snap.workspaceUnknown)\n  );\n\n  const gmailIssue=snap.gmailState!==\"connected\";\n  const title=el(\"connectionHealthTitle\");\n  const detail=el(\"connectionHealthDetail\");\n  const checks=el(\"connectionHealthChecks\");\n  const primary=el(\"connectionHealthPrimary\");\n  const secondary=el(\"connectionHealthSecondary\");\n\n  const googleChip=snap.googleState===\"connected\"&&snap.workspaceReady\n    ? '<span class=\"ok\">✓ Sheet / Drive</span>'\n    : (coreUnknown?'<span class=\"warn\">? Sheet / Drive</span>':'<span class=\"bad\">! Sheet / Drive</span>');\n  const businessChip=snap.businessReady\n    ? '<span class=\"ok\">✓ ธุรกิจ</span>'\n    : (snap.businessUnknown?'<span class=\"warn\">? ธุรกิจ</span>':'<span class=\"bad\">! ธุรกิจ</span>');\n  const gmailChip=snap.gmailState===\"connected\"\n    ? '<span class=\"ok\">✓ Gmail</span>'\n    : (gmailUnknown?'<span class=\"warn\">? Gmail</span>':'<span class=\"warn\">! Gmail</span>');\n\n  if(checks)checks.innerHTML=businessChip+googleChip+gmailChip;\n  if(secondary){secondary.hidden=false;secondary.textContent=\"ตรวจอีกครั้ง\";}\n\n  if(coreUnknown){\n    box.hidden=false;\n    box.dataset.level=\"notice\";\n    if(title)title.textContent=\"ตรวจสถานะการเชื่อมต่อไม่สำเร็จชั่วคราว\";\n    if(detail)detail.textContent=\"ระบบยังไม่เปลี่ยนสิทธิ์หรือการเชื่อมต่อเดิม กรุณาลองตรวจอีกครั้ง\";\n    if(primary){primary.hidden=true;primary.dataset.action=\"\";}\n    DASH_SETUP_BLOCKED=false;\n    return;\n  }\n\n  if(confirmedCoreDisconnect){\n    box.hidden=false;\n    box.dataset.level=\"critical\";\n    if(title)title.textContent=snap.googleState===\"reconnect\"\n      ?\"Google Sheet / Drive ต้องเชื่อมใหม่\"\n      :snap.googleState===\"never\"\n        ?\"Google Sheet / Drive ยังไม่ได้เชื่อม\"\n        :\"การเชื่อมต่อธุรกิจยังไม่พร้อม\";\n    if(detail)detail.textContent=\"ข้อมูลเดิมไม่ได้ถูกลบ ระบบจะให้เชื่อมใหม่เฉพาะเมื่อยืนยันได้ว่าสิทธิ์ไม่พร้อมจริง\";\n    if(primary){\n      primary.hidden=false;\n      primary.dataset.action=\"business\";\n      primary.textContent=snap.googleState===\"reconnect\"?\"เชื่อม Google ใหม่\":\"เชื่อมธุรกิจ / Google\";\n    }\n    DASH_SETUP_BLOCKED=true;\n    return;\n  }\n\n  DASH_SETUP_BLOCKED=false;\n\n  if(gmailUnknown){\n    box.hidden=false;\n    box.dataset.level=\"notice\";\n    if(title)title.textContent=\"ตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว\";\n    if(detail)detail.textContent=\"การเชื่อมต่อเดิมยังไม่ถูกเปลี่ยน ระบบจะไม่บังคับให้เชื่อม Gmail ใหม่จากการตรวจที่ล้มครั้งเดียว\";\n    if(primary){primary.hidden=true;primary.dataset.action=\"\";}\n    return;\n  }\n\n  if(gmailIssue){\n    box.hidden=false;\n    box.dataset.level=\"notice\";\n    if(title)title.textContent=snap.gmailState===\"reconnect\"?\"ต้องเชื่อม Gmail ใหม่\":\"Gmail ยังไม่ได้เชื่อม\";\n    if(detail)detail.textContent=\"Dashboard ใช้งานได้ตามปกติ แต่เอกสารจากอีเมลจะยังไม่เข้าระบบอัตโนมัติ\";\n    if(primary){\n      primary.hidden=false;\n      primary.dataset.action=\"gmail\";\n      primary.textContent=snap.gmailState===\"reconnect\"?\"เชื่อม Gmail ใหม่\":\"เชื่อม Gmail\";\n    }\n    return;\n  }\n\n  box.hidden=true;\n  if(primary){primary.hidden=false;primary.dataset.action=\"\";}\n};\n\nrefreshConnectionHealth=async function({manual=false,loadAfter=false}={}){\n  if(manual)syncStatus(\"syncing\",\"กำลังตรวจการเชื่อมต่อ…\");\n\n  const [googleOk,settingsOk,businessOk,workspaceOk,gmailOk]=await Promise.all([\n    refreshGoogleCoreStatus(),\n    refreshSettings(),\n    refreshBusinesses({quiet:true}),\n    refreshWorkspaceLinks(),\n    refreshGmailConnectionStatus({retries:2,delayMs:400})\n  ]);\n\n  const lkg=v761ReadLkg();\n  const googleState=v761GoogleState();\n  const gmailState=v761GmailState();\n\n  const businessUnknown=businessOk!==true;\n  const workspaceUnknown=workspaceOk!==true&&!(googleState===\"reconnect\"||googleState===\"never\");\n\n  const businessReady=businessOk===true\n    ? currentBusinessConnected()\n    : (CONNECTION_HEALTH?.business===true||lkg.business?.ready===true||coreReadyGraceActive());\n\n  const workspaceReady=workspaceOk===true\n    ? true\n    : (googleState===\"reconnect\"||googleState===\"never\")\n      ? false\n      : (CONNECTION_HEALTH?.workspace===true||lkg.workspace?.ready===true||coreReadyGraceActive());\n\n  const gmailReady=gmailState===\"connected\";\n  const confirmedCoreDisconnect=\n    googleState===\"reconnect\"||\n    googleState===\"never\"||\n    (businessOk===true&&!currentBusinessConnected());\n\n  CONNECTION_HEALTH={\n    ...CONNECTION_HEALTH,\n    checked:true,\n    business:Boolean(businessReady),\n    workspace:Boolean(workspaceReady),\n    gmail:Boolean(gmailReady),\n    gmailReconnect:gmailState===\"reconnect\",\n    businessUnknown,\n    workspaceUnknown,\n    googleUnknown:googleState===\"unknown\",\n    gmailUnknown:gmailState===\"unknown\"\n  };\n\n  if(businessReady)v761WriteLkg({business:{known:true,ready:true,at:Date.now()}});\n  if(workspaceReady)v761WriteLkg({workspace:{...(v761ReadLkg().workspace||{}),known:true,ready:true,at:Date.now()}});\n  renderConnectionHealthBanner();\n\n  if(!confirmedCoreDisconnect){\n    if(manual){\n      const uncertain=googleState===\"unknown\"||gmailState===\"unknown\"||businessUnknown||workspaceUnknown;\n      syncStatus(uncertain?\"error\":\"ok\",uncertain?\"ตรวจบางสถานะไม่ได้ชั่วคราว · การเชื่อมต่อเดิมยังอยู่\":`การเชื่อมต่อพร้อม ${clockTime()}`);\n    }\n    if(loadAfter)await refreshData({manual:true});\n  }else{\n    clearDashboardRetry();\n    syncStatus(\"error\",\"ต้องตรวจการเชื่อมต่อ Google\");\n    hideNetworkBanner();\n  }\n\n  renderOnboarding();\n  applyWorkspaceBranding();\n  return !confirmedCoreDisconnect;\n};\n\nfunction v761ApplySettingsConnectionUi(){\n  const googleState=v761GoogleState();\n  const gmailState=v761GmailState();\n\n  const gs=el(\"setGoogleState\");\n  const ga=el(\"setGoogleAction\");\n  if(gs){\n    const connected=googleState===\"connected\";\n    const unknown=googleState===\"unknown\";\n    gs.className=\"integration-state \"+(connected?\"ok\":unknown?\"warn\":\"bad\");\n    gs.innerHTML=`<span class=\"state-dot\"></span><span>${connected?\"เชื่อมต่อแล้ว\":unknown?\"ตรวจสถานะไม่ได้ชั่วคราว\":googleState===\"reconnect\"?\"ต้องเชื่อมใหม่\":\"ยังไม่ได้เชื่อมต่อ\"}</span>`;\n  }\n  if(ga){\n    if(googleState===\"unknown\"){\n      ga.href=\"#\";\n      ga.textContent=\"ตรวจอีกครั้ง\";\n      ga.className=\"btn\";\n      ga.onclick=async event=>{\n        event.preventDefault();\n        await refreshConnectionHealth({manual:true,loadAfter:false});\n        renderSettings();\n      };\n    }else{\n      ga.href=WORKER+\"/oauth/connect?tenant=\"+encodeURIComponent(TENANT);\n      ga.textContent=googleState===\"connected\"?\"เชื่อมต่อใหม่\":googleState===\"reconnect\"?\"เชื่อม Google ใหม่\":\"เชื่อมต่อ Google\";\n      ga.className=\"btn \"+(googleState===\"connected\"?\"\":\"solid\");\n      ga.onclick=null;\n    }\n  }\n\n  const gm=el(\"setGmailState\");\n  const gma=el(\"setGmailAction\");\n  if(gm){\n    const connected=gmailState===\"connected\";\n    const unknown=gmailState===\"unknown\";\n    gm.className=\"integration-state \"+(connected?\"ok\":gmailState===\"reconnect\"?\"bad\":\"warn\");\n    gm.innerHTML=`<span class=\"state-dot\"></span><span>${connected?\"เชื่อมต่อแล้ว\":unknown?\"ตรวจสถานะไม่ได้ชั่วคราว\":gmailState===\"reconnect\"?\"ต้องเชื่อมใหม่\":\"ยังไม่ได้เชื่อมต่อ\"}</span>`;\n  }\n  if(gma){\n    gma.textContent=gmailState===\"connected\"?\"เปิดกล่องเอกสาร\":gmailState===\"unknown\"?\"ตรวจอีกครั้ง\":gmailState===\"reconnect\"?\"เชื่อม Gmail ใหม่\":\"เชื่อมต่อ Gmail\";\n    gma.onclick=async event=>{\n      event.preventDefault();\n      const state=v761GmailState();\n      if(state===\"connected\"){\n        openPage(\"email\",document.querySelector('[data-p=\"email\"]'));\n        return;\n      }\n      if(state===\"unknown\"){\n        await refreshGmailConnectionStatus({retries:2,delayMs:450});\n        renderSettings();\n        return;\n      }\n      location.href=WORKER+`/gmail/connect?tenant=${encodeURIComponent(TENANT)}&k=${encodeURIComponent(K)}`;\n    };\n  }\n\n  const shortcut=el(\"connBtn\");\n  if(shortcut){\n    if(googleState===\"unknown\"||googleState===\"connected\"){\n      shortcut.style.display=\"none\";\n    }else{\n      shortcut.href=WORKER+\"/oauth/connect?tenant=\"+encodeURIComponent(TENANT);\n      shortcut.style.display=\"flex\";\n    }\n  }\n}\n\nconst v761RenderSettingsCore=renderSettings;\nrenderSettings=function(...args){\n  const out=v761RenderSettingsCore.apply(this,args);\n  v761ApplySettingsConnectionUi();\n  return out;\n};\n\nfunction v761BindEmailConnectionActions(){\n  const state=v761GmailState();\n  const connect=el(\"connectGmail\");\n  if(connect){\n    connect.textContent=state===\"unknown\"?\"ตรวจอีกครั้ง\":state===\"reconnect\"?\"เชื่อม Gmail ใหม่\":\"เชื่อมต่อ Gmail\";\n    connect.onclick=async event=>{\n      event.preventDefault();\n      const now=v761GmailState();\n      if(now===\"unknown\"){\n        await refreshGmailConnectionStatus({retries:2,delayMs:450});\n        renderEmailInbox();\n        return;\n      }\n      location.href=WORKER+`/gmail/connect?tenant=${encodeURIComponent(TENANT)}&k=${encodeURIComponent(K)}`;\n    };\n  }\n\n  const disconnect=el(\"gmailDisconnect\");\n  if(disconnect){\n    disconnect.onclick=async()=>{\n      if(!confirm(\"ยกเลิกการเชื่อมต่อ Gmail? ระบบจะหยุดดึงเอกสารใหม่ แต่ข้อมูลที่บันทึกไว้จะไม่หาย\"))return;\n      const {res}=await emailPost(\"/api/gmail-disconnect\",{});\n      if(!res.ok){alert(\"ยกเลิกการเชื่อมต่อไม่สำเร็จ\");return;}\n      EMAIL_INFO={connected:false,reconnectRequired:false,everConnected:false,statusReason:\"never_connected\"};\n      V761_GMAIL_KNOWN=true;\n      V761_GMAIL_PROBE_ERROR=false;\n      v761WriteGmailLkg();\n      renderEmailInbox();renderOnboarding();renderSettings();\n    };\n  }\n}\n\nfunction v761ApplyEmailUnknownUi(){\n  const state=v761GmailState();\n  if(state!==\"unknown\")return;\n\n  if(el(\"gmailDisconnected\"))el(\"gmailDisconnected\").hidden=false;\n  if(el(\"gmailConnected\"))el(\"gmailConnected\").hidden=true;\n  if(el(\"gmailDisconnectedState\")){\n    el(\"gmailDisconnectedState\").className=\"integration-state\";\n    el(\"gmailDisconnectedState\").innerHTML='<span class=\"state-dot\"></span><span>ตรวจสถานะไม่ได้ชั่วคราว</span>';\n  }\n  if(el(\"emailInboxState\")){\n    el(\"emailInboxState\").textContent=\"ตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว · การเชื่อมต่อเดิมยังไม่ถูกเปลี่ยน\";\n  }\n  if(el(\"emailList\")&&!EMAIL_DOCS.length){\n    el(\"emailList\").innerHTML='<div class=\"email-empty\">ยังตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว<br><br>ระบบจะไม่บังคับให้เชื่อมใหม่ กรุณากด “ตรวจอีกครั้ง”</div>';\n  }\n}\n\nconst v761RenderEmailInboxCore=renderEmailInbox;\nrenderEmailInbox=function(...args){\n  v761CaptureExplicitTruth();\n  const out=v761RenderEmailInboxCore.apply(this,args);\n  v761ApplyEmailUnknownUi();\n  v761BindEmailConnectionActions();\n  return out;\n};\n\nconst v761OpenCompanySetupStepCore=openCompanySetupStep;\nopenCompanySetupStep=async function(step){\n  if(step!==\"owner_gmail\")return v761OpenCompanySetupStepCore(step);\n\n  COMPANY_SETUP_ACTIVE=step;\n  const gate=el(\"companySetupGate\");if(gate)gate.hidden=true;\n  document.body.classList.remove(\"company-setup-required\");\n\n  let state=v761GmailState();\n  if(state===\"connected\"){\n    openPage(\"email\",document.querySelector('[data-p=\"email\"]'));\n    return;\n  }\n\n  if(state===\"unknown\"){\n    syncStatus(\"syncing\",\"กำลังตรวจสถานะ Gmail…\");\n    await refreshGmailConnectionStatus({retries:2,delayMs:450});\n    state=v761GmailState();\n    if(state===\"connected\"){\n      syncStatus(\"ok\",`Gmail พร้อมใช้งาน ${clockTime()}`);\n      openPage(\"email\",document.querySelector('[data-p=\"email\"]'));\n      return;\n    }\n    if(state===\"unknown\"){\n      syncStatus(\"error\",\"ตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว\");\n      alert(\"ยังตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว\\\\n\\\\nระบบยังไม่ได้ตัดการเชื่อมต่อเดิม กรุณาลองอีกครั้งภายหลัง\");\n      return;\n    }\n  }\n\n  // เข้าหน้า OAuth เฉพาะเมื่อ status สำเร็จและยืนยันว่า never/reconnect จริง\n  beginOperation(state===\"reconnect\"?\"เชื่อม Gmail ใหม่\":\"เชื่อม Gmail เจ้าของธุรกิจ\");\n  location.href=WORKER+`/gmail/connect?tenant=${encodeURIComponent(TENANT)}&k=${encodeURIComponent(K)}`;\n};\n\nconsole.info(\"[Dashboard] v7.61 connection/interaction state safety active\");\n";
const loadAnchor="\nload();";
const pos=src.lastIndexOf(loadAnchor);
if(pos<0)throw new Error("หา final load(); ไม่เจอ");
src=src.slice(0,pos)+"\n"+runtime+"\n"+src.slice(pos);

fs.writeFileSync(file,src);
execFileSync(process.execPath,["--check",file],{stdio:"inherit"});

if(fs.existsSync(indexFile)){
  let html=fs.readFileSync(indexFile,"utf8");
  html=html.replace(/\.\/assets\/dashboard\.js\?v=[^"]+/, "./assets/dashboard.js?v=7.61.20260816");
  fs.writeFileSync(indexFile,html);
}

// Static assertions
const out=fs.readFileSync(file,"utf8");
if(!out.includes(MARK))throw new Error("marker หาย");
if(!out.includes("v761GmailState"))throw new Error("Gmail state guard หาย");
if(!out.includes("google_temporarily_unavailable") && !out.includes("refreshGoogleCoreStatus")){
  throw new Error("Google safety guard ไม่ครบ");
}
if(!out.includes("ตรวจสถานะ Gmail ไม่สำเร็จชั่วคราว"))throw new Error("unknown Gmail UI หาย");
if(!out.includes("GET fail ห้ามทำลิงก์ที่เคยใช้ได้หาย"))throw new Error("workspace LKG guard หาย");

console.log("✅ "+MARK+" ready");
console.log("✅ Gmail/Google now use connected / reconnect / never / unknown states");
console.log("✅ transient status failure never becomes a fake reconnect button");
console.log("✅ Sheet/Drive links survive transient refresh failures");
console.log("✅ settings / email / setup actions retry UNKNOWN before OAuth");
