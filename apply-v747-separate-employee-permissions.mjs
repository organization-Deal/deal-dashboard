import fs from "node:fs";
import path from "node:path";
import { execFileSync } from "node:child_process";

const root=process.cwd();
const indexFile=path.join(root,"index.html");
const assets=path.join(root,"assets");
const assetFile=path.join(assets,"employee-permission-split-v747.js");
const guideFile=path.join(assets,"usage-guide-v746.js");
const MARK="EMPLOYEE_PERMISSION_SPLIT_V7_47_20260814";

if(!fs.existsSync(indexFile)) throw new Error("ไม่พบ index.html — ให้รันที่ root ของ deal-dashboard");
if(!fs.existsSync(assets)) fs.mkdirSync(assets,{recursive:true});

fs.writeFileSync(assetFile,"(()=>{\n  \"use strict\";\n  const MARK=\"EMPLOYEE_PERMISSION_SPLIT_V7_47_20260814\";\n  const $=(s,r=document)=>r.querySelector(s);\n  const $$=(s,r=document)=>[...r.querySelectorAll(s)];\n  let VIEW=\"employees\";\n\n  function addStyle(){\n    if($(\"#employeePermissionSplitV747Style\")) return;\n    const s=document.createElement(\"style\");\n    s.id=\"employeePermissionSplitV747Style\";\n    s.textContent=`\n      #biz-team[data-team-view=\"employees\"] #dashboardAccessCard{display:none!important}\n      #biz-team[data-team-view=\"permissions\"] .team-directory-card-v740,\n      #biz-team[data-team-view=\"permissions\"] .team-manual-card-v740{display:none!important}\n\n      .employee-permission-title-v747{\n        margin:0 0 14px;\n        padding:0 0 14px;\n        border-bottom:1px solid #ececf0;\n      }\n      .employee-permission-title-v747 .kicker{\n        font-size:9px;\n        letter-spacing:.08em;\n        color:#86868b;\n        font-weight:800;\n      }\n      .employee-permission-title-v747 h2{\n        margin:4px 0 3px;\n        font-size:22px;\n        letter-spacing:-.025em;\n        color:#111;\n      }\n      .employee-permission-title-v747 p{\n        margin:0;\n        color:#6e6e73;\n        font-size:10.5px;\n        line-height:1.5;\n      }\n\n      #biz-team[data-team-view=\"employees\"] .team-directory-card-v740{\n        display:block!important;\n        width:100%!important;\n        max-width:none!important;\n        margin:0!important;\n      }\n      #biz-team[data-team-view=\"employees\"] .team-directory-card-v740>h3,\n      #biz-team[data-team-view=\"employees\"] .team-directory-card-v740>.cs{\n        display:none!important;\n      }\n      #biz-team[data-team-view=\"employees\"] .team-manual-card-v740{\n        width:100%!important;\n        max-width:none!important;\n      }\n\n      #biz-team[data-team-view=\"permissions\"] #dashboardAccessCard{\n        display:block!important;\n        width:100%!important;\n        max-width:none!important;\n        margin:0!important;\n      }\n\n      #dashboardAccessCard .dash-access-kicker{display:none!important}\n      #dashboardAccessCard .dash-access-head h3{\n        font-size:18px!important;\n        margin:0 0 3px!important;\n      }\n      #dashboardAccessCard .dash-access-head .cs{\n        font-size:10px!important;\n      }\n\n      .subnavlink[data-biz=\"permissions\"].active{\n        background:var(--nav-active,#ededf0);\n      }\n\n      @media(max-width:760px){\n        .employee-permission-title-v747 h2{font-size:20px}\n      }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function textNode(button,label){\n    if(!button) return;\n    const span=button.querySelector(\"span:last-child\");\n    if(span) span.textContent=label;\n    else button.textContent=label;\n  }\n\n  function setupNav(){\n    const desktopTeam=$('#businessMenu [data-biz=\"team\"]');\n    if(desktopTeam){\n      textNode(desktopTeam,\"ข้อมูลพนักงาน\");\n      desktopTeam.dataset.teamView=\"employees\";\n      if(!$('#businessMenu [data-biz=\"permissions\"]')){\n        const p=desktopTeam.cloneNode(true);\n        p.dataset.biz=\"permissions\";\n        p.dataset.teamView=\"permissions\";\n        p.classList.remove(\"active\");\n        textNode(p,\"สิทธิ์การใช้งาน\");\n        desktopTeam.insertAdjacentElement(\"afterend\",p);\n      }\n    }\n\n    const mobileTeam=$('[data-mobile-biz=\"team\"]');\n    if(mobileTeam){\n      textNode(mobileTeam,\"ข้อมูลพนักงาน\");\n      mobileTeam.dataset.teamView=\"employees\";\n      if(!$('[data-mobile-biz=\"permissions\"]')){\n        const p=mobileTeam.cloneNode(true);\n        p.dataset.mobileBiz=\"permissions\";\n        p.dataset.teamView=\"permissions\";\n        textNode(p,\"สิทธิ์การใช้งาน\");\n        mobileTeam.insertAdjacentElement(\"afterend\",p);\n      }\n    }\n  }\n\n  function ensureViewHeader(){\n    const panel=$(\"#biz-team\");\n    const grid=panel?.querySelector(\".biz-grid\");\n    if(!panel||!grid) return null;\n    let head=$(\"#employeePermissionHeaderV747\");\n    if(!head){\n      head=document.createElement(\"div\");\n      head.id=\"employeePermissionHeaderV747\";\n      head.className=\"employee-permission-title-v747\";\n      grid.insertBefore(head,grid.firstChild);\n    }\n    return head;\n  }\n\n  function updateHeader(){\n    const head=ensureViewHeader();\n    if(!head) return;\n    if(VIEW===\"permissions\"){\n      head.innerHTML=`\n        <div class=\"kicker\">ACCESS CONTROL</div>\n        <h2>สิทธิ์การใช้งาน</h2>\n        <p>กำหนดว่าใครเป็นผู้อนุมัติ ใครเป็นบัญชี / การเงิน หรือใครเข้าดู Dashboard ได้ โดยใช้รายชื่อพนักงานจาก LINE ที่มีอยู่แล้ว</p>`;\n    }else{\n      head.innerHTML=`\n        <div class=\"kicker\">EMPLOYEE DIRECTORY</div>\n        <h2>ข้อมูลพนักงาน</h2>\n        <p>จัดการรายชื่อพนักงานจาก LINE ข้อมูลรับเงิน และสถานะการลงทะเบียนของแต่ละคน</p>`;\n    }\n  }\n\n  function renameCards(){\n    const directory=$(\"#teamList\")?.closest(\".card\");\n    if(directory){\n      directory.classList.add(\"team-directory-card-v740\");\n      const h=directory.querySelector(\"h3\");\n      const sub=directory.querySelector(\".cs\");\n      if(h) h.textContent=\"ข้อมูลพนักงาน\";\n      if(sub) sub.textContent=\"รายชื่อพนักงานจาก LINE พร้อมข้อมูลรับเงินและสถานะลงทะเบียน\";\n    }\n\n    const access=$(\"#dashboardAccessCard\");\n    if(access){\n      const h=access.querySelector(\".dash-access-head h3\");\n      const sub=access.querySelector(\".dash-access-head .cs\");\n      if(h) h.textContent=\"เพิ่มสิทธิ์ให้พนักงาน\";\n      if(sub) sub.textContent=\"เลือกหน้าที่ → เลือกพนักงานจาก LINE → บันทึกสิทธิ์\";\n      const wf=$(\"#workflowInlineV736\");\n      if(wf){\n        const b=wf.querySelector(\"summary div b\");\n        const sm=wf.querySelector(\"summary div small\");\n        if(b) b.textContent=\"Workflow ของสิทธิ์การใช้งาน\";\n        if(sm) sm.textContent=\"ตั้งค่าด่านอนุมัติและบัญชีของบริษัท ใช้ร่วมกับผู้ที่ได้รับสิทธิ์ด้านบน\";\n      }\n    }\n  }\n\n  function setActiveNav(){\n    $$('[data-biz]').forEach(btn=>{\n      if(btn.dataset.biz===\"team\") btn.classList.toggle(\"active\",VIEW===\"employees\");\n      if(btn.dataset.biz===\"permissions\") btn.classList.toggle(\"active\",VIEW===\"permissions\");\n    });\n  }\n\n  function updateUrl(){\n    try{\n      const q=new URLSearchParams(location.search);\n      q.set(\"page\",\"business\");\n      q.set(\"biz\",VIEW===\"permissions\"?\"permissions\":\"team\");\n      history.replaceState(null,\"\",`${location.pathname}?${q.toString()}${location.hash||\"\"}`);\n    }catch{}\n  }\n\n  function showView(view,{writeUrl=true}={}){\n    VIEW=view===\"permissions\"?\"permissions\":\"employees\";\n    const panel=$(\"#biz-team\");\n    if(!panel) return;\n    panel.dataset.teamView=VIEW;\n\n    // ใช้ business panel เดิม แต่แยกเนื้อหาเป็นคนละหน้าตามเมนู\n    document.querySelectorAll(\".business-tab\").forEach(x=>x.classList.remove(\"show\"));\n    panel.classList.add(\"show\");\n\n    setupNav();\n    renameCards();\n    updateHeader();\n    setActiveNav();\n\n    if(writeUrl) updateUrl();\n\n    // ให้หน้า permission render ค่าล่าสุดของ access control\n    try{\n      if(VIEW===\"permissions\" && typeof renderBusiness===\"function\") renderBusiness();\n    }catch{}\n\n    window.scrollTo({top:0,behavior:\"instant\"});\n  }\n\n  function patchSetBusinessTab(){\n    try{\n      if(typeof setBusinessTab!==\"function\" || setBusinessTab.__v747) return;\n      const original=setBusinessTab;\n      const wrapped=function(tab,...rest){\n        if(tab===\"permissions\"){\n          original.call(this,\"team\",...rest);\n          setTimeout(()=>showView(\"permissions\",{writeUrl:false}),0);\n          return;\n        }\n        if(tab===\"team\"){\n          original.call(this,\"team\",...rest);\n          setTimeout(()=>showView(\"employees\",{writeUrl:false}),0);\n          return;\n        }\n        return original.call(this,tab,...rest);\n      };\n      wrapped.__v747=true;\n      setBusinessTab=wrapped;\n    }catch(e){console.warn(\"v7.47 setBusinessTab\",e)}\n  }\n\n  function patchGuideLabels(){\n    const modal=$(\"#usageGuideV746\");\n    if(!modal) return;\n    $$(\"*\",modal).forEach(node=>{\n      if(node.children.length) return;\n      const t=node.textContent||\"\";\n      if(t.includes(\"ทีมและสิทธิ์\")) node.textContent=t.replace(/ทีมและสิทธิ์/g,\"สิทธิ์การใช้งาน\");\n      if(t.includes(\"กำหนดทีมและสิทธิ์\")) node.textContent=t.replace(/กำหนดทีมและสิทธิ์/g,\"กำหนดสิทธิ์การใช้งาน\");\n    });\n  }\n\n  document.addEventListener(\"click\",e=>{\n    const permission=e.target.closest('[data-biz=\"permissions\"],[data-mobile-biz=\"permissions\"]');\n    if(permission){\n      e.preventDefault();\n      e.stopImmediatePropagation();\n      showView(\"permissions\");\n      const sheet=$(\"#mobileMoreBackdrop\");\n      if(sheet) sheet.hidden=true;\n      return;\n    }\n\n    const employee=e.target.closest('[data-biz=\"team\"],[data-mobile-biz=\"team\"]');\n    if(employee && employee.dataset.teamView===\"employees\"){\n      // ปล่อย router เดิมเปิด business page แต่กำหนด view หลังจากนั้น\n      setTimeout(()=>showView(\"employees\"),25);\n    }\n\n    if(e.target.closest(\"button,a,[role=button]\")?.textContent?.includes(\"วิธีใช้งาน\")){\n      setTimeout(patchGuideLabels,80);\n    }\n  },true);\n\n  function boot(){\n    addStyle();\n    setupNav();\n    patchSetBusinessTab();\n    renameCards();\n\n    const q=new URLSearchParams(location.search);\n    if(q.get(\"page\")===\"business\"){\n      showView(q.get(\"biz\")===\"permissions\"?\"permissions\":\"employees\",{writeUrl:false});\n    }else{\n      const panel=$(\"#biz-team\");\n      if(panel) panel.dataset.teamView=\"employees\";\n    }\n  }\n\n  if(document.readyState===\"loading\") document.addEventListener(\"DOMContentLoaded\",boot,{once:true});\n  else boot();\n  window.addEventListener(\"load\",()=>setTimeout(boot,120),{once:true});\n  console.info(MARK);\n})();");
execFileSync(process.execPath,["--check",assetFile],{stdio:"inherit"});

// อัปเดตคำในวิธีใช้งานให้ตรงโครงใหม่ ถ้ามี asset v7.46 อยู่
if(fs.existsSync(guideFile)){
  let guide=fs.readFileSync(guideFile,"utf8");
  guide=guide.replace(
    '["ทีมและสิทธิ์","ตั้งผู้อนุมัติ บัญชี/การเงิน คนดู Dashboard และ Workflow"],',
    '["ข้อมูลพนักงาน","ดูรายชื่อพนักงานจาก LINE ข้อมูลรับเงิน และสถานะลงทะเบียน"],["สิทธิ์การใช้งาน","กำหนดผู้อนุมัติ บัญชี/การเงิน คนดู Dashboard และ Workflow"],'
  );
  guide=guide.replace(/กำหนดทีมและสิทธิ์/g,"กำหนดสิทธิ์การใช้งาน");
  fs.writeFileSync(guideFile,guide);
  execFileSync(process.execPath,["--check",guideFile],{stdio:"inherit"});
}

let html=fs.readFileSync(indexFile,"utf8");
if(!html.includes(MARK)){
  html=html.replace("</body>",`<!-- ${MARK} -->
<script src="./assets/employee-permission-split-v747.js?v=7.47"></script>
</body>`);
  fs.writeFileSync(indexFile,html);
}
console.log("✅ "+MARK+" ready");
