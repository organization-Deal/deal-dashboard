{
  "name": "deal-dashboard-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "node apply-v735-line-group-ui.mjs && wrangler deploy --config ./wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "4.115.0"
  }
}
